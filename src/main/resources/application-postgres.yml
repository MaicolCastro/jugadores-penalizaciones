spring:
  r2dbc:
    url: r2dbc:postgresql://postgres:5432/parcial
    username: parcial
    password: parcial
    pool:
      enabled: true
      initial-size: 5
      max-size: 20
      max-idle-time: 30m

app:
  db:
    schema: classpath:db/schema-postgres.sql
